import styled from 'styled-components'

export const Container = styled.div`
  height: auto;
  max-width: 1140px;
  overflow: hidden;
  padding: 120px 70px 30px;
`

export const ContainerBackgroundOverlay = styled.div`
  background-color: rgba(0, 0, 0, 1);
  height: 100%;
  opacity: 0.5;
  position: fixed;
  width: 100%;
`
